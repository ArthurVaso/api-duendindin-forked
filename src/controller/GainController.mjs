import { Gain } from "../model/Gain.mjs";
import sequelize from "sequelize";

export const deleteAllCategoryEarnings = async (userId, categoryId) => { 
    try{
        
    }catch(err){
        
    }
}